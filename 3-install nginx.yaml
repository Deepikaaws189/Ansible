- name: install nginx
  hosts: web
  become: yes
  tasks:
    - name: install nginx package
      ansible.builtin.package:
      name: nginx
      state: present
    - name: Service start
      ansible.builtin.service:
      name: nginx
      state: started
      enabled: yes